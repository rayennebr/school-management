server:
  port: 9090
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rayennebr/school-management-configuration
          clone-on-start: true

